import Link from 'next/link'; import { db } from '@/lib/prisma'; import { getCurrentUser } from '@/lib/auth'; import { notFound, redirect } from 'next/navigation'; import { money } from '@/lib/config'; import { Footer } from '@/components/footer'; import CancelOrderButton from '@/components/cancel-order-button';
export default async function OrderDetails({params}:{params:Promise<{orderNumber:string}>}){const user=await getCurrentUser();if(!user)redirect('/account/login');const {orderNumber}=await params;const order=await db.order.findUnique({where:{orderNumber},select:{id:true,orderNumber:true,userId:true,createdAt:true,status:true,shippingAddressJson:true,paymentMethod:true,paymentStatus:true,shippingTotal:true,subtotal:true,grandTotal:true,currency:true,trackingNumber:true,items:{select:{id:true,name:true,quantity:true,totalPrice:true},},events:{select:{id:true,status:true,message:true,createdAt:true},orderBy:{createdAt:'asc'}},paymentTransactions:{select:{id:true,status:true,amount:true},orderBy:{createdAt:'desc'}}}});if(!order||order.userId!==user.id)return notFound();let address:any={};try{address=JSON.parse(order.shippingAddressJson)}catch{};const refundable=order.paymentTransactions.filter(t=>t.status==='refunded').reduce((s,t)=>s+t.amount,0);return <><main className="section"><div className="container"><Link className="textLink" href="/account">← Back to account</Link><div className="sectionHead"><div><span className="muted">ORDER</span><h1 className="h2">#{order.orderNumber}</h1><p className="muted">{order.createdAt.toLocaleString()}</p></div><div style={{display:'flex',gap:8,alignItems:'center'}}><span className="pill">{order.status}</span>{(order.status==='PENDING'||order.status==='CONFIRMED')&&<CancelOrderButton orderNumber={order.orderNumber}/>}</div></div><div className="grid twoColumn"><section><div className="card" style={{padding:20}}><h3>Items</h3>{order.items.map(i=><div className="summaryLine" key={i.id}><span>{i.name} × {i.quantity}</span><strong>{money(i.totalPrice,order.currency)}</strong></div>)}<div className="summaryLine"><span>Subtotal</span><strong>{money(order.subtotal,order.currency)}</strong></div><div className="summaryLine"><span>Shipping</span><strong>{money(order.shippingTotal,order.currency)}</strong></div><div className="summaryLine total"><span>Total</span><strong>{money(order.grandTotal,order.currency)}</strong></div>{refundable>0&&<p className="muted" style={{marginTop:10}}>Refunded so far: {money(refundable,order.currency)}</p>}</div><div className="card" style={{padding:20,marginTop:12}}><h3>Timeline</h3>{order.events.map(e=><div className="timelineItem" key={e.id}><span className="dot"/><div><strong>{e.status}</strong><p className="muted">{e.message}</p><small className="muted">{e.createdAt.toLocaleString()}</small></div></div>)}</div></section><aside className="card" style={{padding:20,height:'fit-content'}}><h3>Delivery</h3><p className="muted">{address.firstName||''} {address.lastName||''}<br/>{address.line1||''}{address.line2?<><br/>{address.line2}</>:null}<br/>{address.city||''}{address.region?`, ${address.region}`:''}<br/>{address.country||''}</p><h3>Payment</h3><p className="muted">{order.paymentMethod} · {order.paymentStatus}</p>{order.trackingNumber&&<><h3>Tracking</h3><p>{order.trackingNumber}</p></>}</aside></div></div></main><Footer/></>}

import Link from 'next/link'; import { getCurrentUser } from '@/lib/auth'; import { db } from '@/lib/prisma'; import { money } from '@/lib/config'; import { Footer } from '@/components/footer';
export default async function Account(){const u=await getCurrentUser();if(!u)return <><main className="section"><div className="container"><span className="muted">ACCOUNT</span><h1 className="h2">Your account</h1><div className="card" style={{padding:24,maxWidth:560}}><h3>Sign in or create an account</h3><p className="muted">Track orders, manage addresses, save favorites and leave reviews.</p><div style={{display:'flex',gap:10}}><Link className="btn" href="/account/login">Sign in</Link><Link className="btn secondary" href="/account/register">Create account</Link></div></div></div></main><Footer/></>;
const orders=await db.order.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'},take:20,include:{items:true}});const addresses=await db.address.findMany({where:{userId:u.id},orderBy:{isDefault:'desc'}});return <><main className="section"><div className="container"><div className="sectionHead"><div><span className="muted">ACCOUNT</span><h1 className="h2">Welcome, {u.name}.</h1></div><form action="/api/auth/logout" method="post"><button className="btn secondary">Sign out</button></form></div><div className="grid accountStats"><div className="card stat"><span className="muted">Orders</span><strong>{orders.length}</strong></div><div className="card stat"><span className="muted">Saved addresses</span><strong>{addresses.length}</strong></div><div className="card stat"><span className="muted">Email</span><strong style={{fontSize:16}}>{u.email}</strong></div></div><div className="grid twoColumn"><section><div className="sectionHead small"><h3>Recent orders</h3><span className="muted">{orders.length}</span></div>{!orders.length?<div className="card empty"><p className="muted">No orders yet.</p><Link href="/shop" className="btn">Start shopping</Link></div>:<div className="grid" style={{gap:10}}>{orders.map(o=><Link href={`/account/orders/${o.orderNumber}`} className="card orderCard" key={o.id}><div><strong>#{o.orderNumber}</strong><span className="muted">{o.createdAt.toLocaleDateString()}</span></div><div><span className="pill">{o.status}</span><strong>{money(o.grandTotal,o.currency)}</strong></div></Link>)}</div>}</section><section><div className="sectionHead small"><h3>Addresses</h3></div>{addresses.length?<div className="grid" style={{gap:10}}>{addresses.map(a=><div className="card" style={{padding:18}} key={a.id}><div style={{display:'flex',justifyContent:'space-between'}}><strong>{a.label||'Address'}</strong>{a.isDefault&&<span className="pill">Default</span>}</div><p className="muted">{a.firstName} {a.lastName}<br/>{a.line1}{a.line2&&<><br/>{a.line2}</>}<br/>{a.city}{a.region?`, ${a.region}`:''}<br/>{a.country}</p></div>)}</div>:<div className="card empty"><p className="muted">No saved addresses.</p></div>}</section></div></div></main><Footer/></>}

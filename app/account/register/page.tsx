import Link from 'next/link'; import { register } from './server';
export default async function Register({searchParams}:{searchParams:Promise<{error?:string}>}){const sp=await searchParams;return <main className="section"><div className="container" style={{maxWidth:520}}><span className="muted">ACCOUNT</span><h1 className="h2" style={{fontSize:46}}>Create account</h1>{sp.error==='invalid'&&<div className="alert danger">Please enter a valid name, email and password of at least 8 characters.</div>}{sp.error==='exists'&&<div className="alert danger">An account with this email already exists.</div>}<form action={register} className="card" style={{padding:24,marginTop:20}}><input className="input" name="name" placeholder="Full name" required/><input className="input" name="email" type="email" placeholder="Email" required style={{marginTop:10}}/><input className="input" name="password" type="password" placeholder="Password (8+ characters)" required minLength={8} style={{marginTop:10}}/><button className="btn" style={{width:'100%',marginTop:14}}>Create account</button></form><p className="muted">Already have an account? <Link href="/account/login">Sign in</Link></p></div></main>}

import { requirePermission } from '@/lib/auth';
import { db } from '@/lib/prisma';
export default async function Activity(){await requirePermission('activity.view');const rows=await db.auditLog.findMany({include:{actor:true},orderBy:{createdAt:'desc'},take:200});return <div><div className="sectionHead"><div><span className="muted">SECURITY</span><h1 className="h2">Activity log</h1></div><span className="pill">{rows.length} events</span></div><div className="card" style={{padding:8,overflowX:'auto'}}><table className="table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>Reference</th><th>Metadata</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td>{a.createdAt.toLocaleString()}</td><td>{a.actor?.email||'System'}</td><td>{a.action}</td><td>{a.entity}</td><td>{a.entityId||'—'}</td><td className="muted">{a.metadataJson||'—'}</td></tr>)}</tbody></table></div></div>}

import { requirePermission } from '@/lib/auth';
import { db } from '@/lib/prisma';
export default async function Customers(){await requirePermission('customers.view');const rows=await db.user.findMany({where:{role:'CUSTOMER'},include:{_count:{select:{orders:true,reviews:true}}},orderBy:{createdAt:'desc'}});return <div><div className="sectionHead"><div><span className="muted">PEOPLE</span><h1 className="h2">Customers</h1></div><span className="pill">{rows.length}</span></div><div className="card" style={{padding:8,overflowX:'auto'}}><table className="table"><thead><tr><th>Customer</th><th>Email</th><th>Orders</th><th>Reviews</th><th>Status</th><th>Joined</th></tr></thead><tbody>{rows.map(c=><tr key={c.id}><td><strong>{c.name}</strong><div className="muted">{c.phone||'No phone'}</div></td><td>{c.email}</td><td>{c._count.orders}</td><td>{c._count.reviews}</td><td><span className="pill">{c.isActive?'Active':'Disabled'}</span></td><td>{c.createdAt.toLocaleDateString()}</td></tr>)}</tbody></table></div></div>}

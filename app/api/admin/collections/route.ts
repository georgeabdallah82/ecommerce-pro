import { db } from '@/lib/prisma'; import { requirePermission } from '@/lib/auth'; import { audit } from '@/lib/audit'; import { json, slugify } from '@/lib/utils'
export async function GET(){try{await requirePermission('collections.view');return json(await db.collection.findMany({include:{_count:{select:{products:true}}},orderBy:{sortOrder:'asc'}}))}catch(e){return json({error:e instanceof Error?e.message:'Forbidden'},{status:403})}}
export async function POST(req:Request){try{const actor=await requirePermission('collections.manage');const b=await req.json();const name=String(b.name||'').trim();if(!name)return json({error:'Name required'},{status:400});const c=await db.collection.create({data:{name,slug:slugify(String(b.slug||name)),description:b.description||null,imageUrl:b.imageUrl||null,sortOrder:Number(b.sortOrder)||0}});if(Array.isArray(b.productIds)&&b.productIds.length)await db.collectionProduct.createMany({data:b.productIds.map((productId:string,i:number)=>({collectionId:c.id,productId,sortOrder:i}))});await audit(actor.id,'collection.created','Collection',c.id,{name});return json({collection:c},{status:201})}catch(e){return json({error:e instanceof Error?e.message:'Unable to create collection'},{status:400})}}

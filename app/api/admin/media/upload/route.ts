import { requirePermission } from '@/lib/auth'; import { audit } from '@/lib/audit'; import { db } from '@/lib/prisma'; import { json } from '@/lib/utils'; import { mkdir, writeFile } from 'fs/promises'; import path from 'path'
export async function POST(req:Request){try{const actor=await requirePermission('media.manage');const form=await req.formData();const file=form.get('file');if(!(file instanceof File))return json({error:'File required'},{status:400});if(!file.type.startsWith('image/'))return json({error:'Only image files are allowed'},{status:400});if(file.size>5*1024*1024)return json({error:'Maximum file size is 5 MB'},{status:400});const ext=(file.name.split('.').pop()||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'');const name=`${Date.now()}-${Math.random().toString(36).slice(2,8)}.${ext}`;const dir=path.join(process.cwd(),'public','uploads');await mkdir(dir,{recursive:true});await writeFile(path.join(dir,name),Buffer.from(await file.arrayBuffer()));const asset=await db.mediaAsset.create({data:{url:`/uploads/${name}`,name:file.name,alt:String(form.get('alt')||file.name),mimeType:file.type,sizeBytes:file.size}});await audit(actor.id,'media.uploaded','MediaAsset',asset.id,{name:file.name,size:file.size});return json({asset},{status:201})}catch(e){return json({error:e instanceof Error?e.message:'Unable to upload image'},{status:400})}}

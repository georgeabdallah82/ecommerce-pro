import {db} from '@/lib/prisma'; import {requirePermission} from '@/lib/auth'; import {audit} from '@/lib/audit'; import {json} from '@/lib/utils'; import {defaultNavigation,defaultSections,defaultTheme} from '@/lib/theme'
export async function GET(){try{await requirePermission('content.view');const [theme,sections,navigation]=await Promise.all([db.setting.findUnique({where:{key:'theme.config'}}),db.setting.findUnique({where:{key:'theme.sections'}}),db.setting.findUnique({where:{key:'navigation.main'}})]);return json({theme:theme?JSON.parse(theme.value):defaultTheme,sections:sections?JSON.parse(sections.value):defaultSections,navigation:navigation?JSON.parse(navigation.value):defaultNavigation})}catch(e){return json({error:e instanceof Error?e.message:'Forbidden'},{status:403})}}
export async function PATCH(req:Request){try{const actor=await requirePermission('content.manage');const b=await req.json();const theme=b.theme||defaultTheme;const sections=b.sections||defaultSections;const navigation=b.navigation||defaultNavigation;await db.$transaction([db.setting.upsert({where:{key:'theme.config'},create:{key:'theme.config',value:JSON.stringify(theme)},update:{value:JSON.stringify(theme)}}),db.setting.upsert({where:{key:'theme.sections'},create:{key:'theme.sections',value:JSON.stringify(sections)},update:{value:JSON.stringify(sections)}}),db.setting.upsert({where:{key:'navigation.main'},create:{key:'navigation.main',value:JSON.stringify(navigation)},update:{value:JSON.stringify(navigation)}})]);await audit(actor.id,'theme.updated','Theme','theme.config',{sections:sections.length,navigation:navigation.length,preset:theme?.presets?.active||null});return json({theme,sections,navigation})}catch(e){return json({error:e instanceof Error?e.message:'Unable to save theme'},{status:400})}}

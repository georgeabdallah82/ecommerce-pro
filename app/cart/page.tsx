'use client'
import Link from 'next/link'; import { useCart, keyOf } from '@/components/cart-provider'; import { Footer } from '@/components/footer'; import { money } from '@/lib/config'
export default function Cart(){const {items,updateQty,removeItem,subtotal}=useCart();return <><main className="section"><div className="container"><div className="sectionHead"><div><span className="muted">YOUR BAG</span><h1 className="h2">Cart</h1></div>{items.length>0&&<span className="pill">{items.reduce((a,b)=>a+b.quantity,0)} items</span>}</div>{!items.length?<div className="card empty"><h3>Your cart is empty.</h3><p className="muted">Discover products and add them here for a fast checkout.</p><Link className="btn" href="/shop">Continue shopping</Link></div>:<div className="split"><div className="grid" style={{gap:10}}>{items.map(item=><div className="card cartRow" key={keyOf(item)}><img src={item.image||'/placeholder-product.svg'} alt=""/><div className="cartInfo"><strong>{item.name}</strong><span className="muted">{item.sku}</span><span className="price">{money(item.price)}</span></div><div className="cartControls"><input className="input" type="number" min="1" max="99" value={item.quantity} onChange={e=>updateQty(keyOf(item),Math.max(1,Math.min(99,Number(e.target.value)||1)))}/><button className="textButton" onClick={()=>removeItem(keyOf(item))}>Remove</button></div></div>)}</div><aside className="card summaryCard"><span className="muted">SUMMARY</span><div className="summaryLine"><span>Subtotal</span><strong>{money(subtotal)}</strong></div><div className="summaryLine"><span>Shipping</span><span>Calculated at checkout</span></div><div className="summaryLine total"><span>Total</span><strong>{money(subtotal)}</strong></div><Link className="btn" href="/checkout">Checkout</Link><Link className="btn secondary" href="/shop">Continue shopping</Link></aside></div>}</div></main><Footer/></>}

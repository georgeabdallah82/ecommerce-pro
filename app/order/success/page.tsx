import Link from 'next/link'; import { notFound } from 'next/navigation';
export default async function Success({searchParams}:{searchParams:Promise<{order?:string}>}){const {order}=await searchParams;if(!order)notFound();return <main className="section"><div className="container"><div className="card successCard"><span className="pill">ORDER CONFIRMED</span><h1 className="h2">Thank you for your order.</h1><p className="body muted">Your order number is <strong>#{order}</strong>. Keep it for your records.</p><div style={{display:'flex',gap:10,flexWrap:'wrap'}}><Link className="btn" href="/shop">Continue shopping</Link><Link className="btn secondary" href="/account">View account</Link></div></div></div></main>}

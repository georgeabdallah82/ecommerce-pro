import Link from 'next/link'; import { notFound } from 'next/navigation'; import { getCurrentUser } from '@/lib/auth';
export default async function Success({searchParams}:{searchParams:Promise<{order?:string}>}){const {order}=await searchParams;if(!order)notFound();const user=await getCurrentUser();return <main className="section"><div className="container"><div className="card successCard"><span className="pill">ORDER CONFIRMED</span><h1 className="h2">Thank you for your order.</h1><p className="body muted">Your order number is <strong>#{order}</strong>. Keep it for your records.</p><div style={{display:'flex',gap:10,flexWrap:'wrap'}}><Link className="btn" href="/shop">Continue shopping</Link>{user?<><Link className="btn secondary" href={`/account/orders/${encodeURIComponent(order)}`}>View order</Link><Link className="btn secondary" href="/account">View account</Link></>:<Link className="btn secondary" href="/account/register">Create an account to track orders</Link>}</div></div></div></main>}

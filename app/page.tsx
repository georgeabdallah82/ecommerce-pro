import Link from 'next/link'
import {db} from '@/lib/prisma'
import {getThemeState} from '@/lib/theme'
import {ProductCard} from '@/components/product-card'
import {Footer} from '@/components/footer'
import {ArrowRight,ChevronRight,Play} from 'lucide-react'
export const dynamic='force-dynamic'

function sectionStyle(theme:any,s:any){return {background:s.background==='primary'?theme.colors.primary:s.background==='secondary'?theme.colors.secondary:s.background==='dark'?'#15120f':s.background==='gradient'?`linear-gradient(135deg, ${theme.colors.secondary}, ${theme.colors.background})`:'transparent',color:s.background==='primary'||s.background==='dark'?'#fff':theme.colors.text}}
function reveal(theme:any,s:any){return theme.animations?.enabled?`reveal-${s.animation||theme.animations.reveal||'fade-up'}`:''}

export default async function Home(){
 const {theme,sections}=await getThemeState()
 const [products,collections]=await Promise.all([
  db.product.findMany({where:{status:'ACTIVE'},include:{images:true,category:true},orderBy:[{featured:'desc'},{createdAt:'desc'}],take:32}),
  db.collection.findMany({where:{isActive:true},take:16,orderBy:{sortOrder:'asc'}})
 ])
 return <><main>{sections.filter((s:any)=>s.enabled).map((section:any)=>{const s=section.settings||{};const style=sectionStyle(theme,s);const anim=reveal(theme,s)
   if(section.type==='announcement') return <div key={section.id} className="storeAnnouncement" style={{background:s.background==='secondary'?theme.colors.secondary:theme.colors.primary,color:s.textColor||theme.colors.buttonText||'#fff'}}>{s.link?<Link href={s.link} style={{color:'inherit',textDecoration:'none'}}>{s.text||theme.announcement?.text||'Free shipping on orders over $50'}</Link>:(s.text||theme.announcement?.text||'Free shipping on orders over $50')}</div>
   if(section.type==='header') return null
   if(section.type==='hero') {const desktop=s.desktopImageUrl||s.imageUrl||'';const mobile=s.mobileImageUrl||desktop;const positionMap:any={'top-left':['flex-start','flex-start'],'top-center':['flex-start','center'],'top-right':['flex-start','flex-end'],'center-left':['center','flex-start'],'center':['center','center'],'center-right':['center','flex-end'],'bottom-left':['flex-end','flex-start'],'bottom-center':['flex-end','center'],'bottom-right':['flex-end','flex-end']};const [vAlign,hAlign]=positionMap[s.contentPosition||'center-left']||positionMap['center-left'];const overlayColor=s.overlayColor||'#000000';const op=Math.max(0,Math.min(1,Number(s.overlay??0.24)));const rgb=(overlayColor||'#000000').replace('#','');const rv=rgb.length===3?rgb.split('').map((x:string)=>x+x).join(''):rgb;const overlay= s.overlayStyle==='bottom-gradient'?`linear-gradient(to top,rgba(${parseInt(rv.slice(0,2),16)||0},${parseInt(rv.slice(2,4),16)||0},${parseInt(rv.slice(4,6),16)||0},${op}),transparent 70%)`:s.overlayStyle==='full-gradient'?`linear-gradient(135deg,rgba(${parseInt(rv.slice(0,2),16)||0},${parseInt(rv.slice(2,4),16)||0},${parseInt(rv.slice(4,6),16)||0},${op}),rgba(${parseInt(rv.slice(0,2),16)||0},${parseInt(rv.slice(2,4),16)||0},${parseInt(rv.slice(4,6),16)||0},${op*0.35}))`:s.overlayStyle==='soft-vignette'?`radial-gradient(circle at center,transparent 25%,rgba(${parseInt(rv.slice(0,2),16)||0},${parseInt(rv.slice(2,4),16)||0},${parseInt(rv.slice(4,6),16)||0},${op}) 100%)`:s.overlayStyle==='none'?'none':`rgba(${parseInt(rv.slice(0,2),16)||0},${parseInt(rv.slice(2,4),16)||0},${parseInt(rv.slice(4,6),16)||0},${op})`;return <section className={`storeSection heroSection ${anim} ${s.fullBleed===false?'heroContained':'heroFull'}`} key={section.id} style={style}><div className="heroBanner" style={{minHeight:s.imageHeightMode==='fixed'?Number(s.minHeight||640):undefined,borderRadius:Number(s.borderRadius||0),alignItems:vAlign,justifyContent:hAlign,aspectRatio:s.imageHeightMode==='fixed'||!desktop?undefined:'16 / 7'}}>{desktop&&<><div className="heroBannerOverlay" style={{background:overlay}}/><picture className="heroBannerMedia"><source media="(max-width: 749px)" srcSet={mobile}/><img src={desktop} alt={s.imageAlt||s.heading||theme.brandName} style={{width:'100%',height:'100%',objectFit:s.imageHeightMode==='adapt'?'cover':(s.imageFit||'cover'),objectPosition:`${s.focalX??50}% ${s.focalY??50}%`,...(s.parallax?{transform:'scale(1.05)'}:{})}}/></picture></>} {!desktop&&<div className="heroBannerPlaceholder"/>}<div className={`heroBannerContent ${s.contentBox?'withBox':''}`} style={{maxWidth:Number(s.contentWidth||620),textAlign:s.textAlign||'left'}}><span className="pill"><span className="heroDot"/> {s.eyebrow||'NEW COLLECTION'}</span><h1 className="h1">{s.heading||'Make your store impossible to ignore.'}</h1><p className="body" style={{maxWidth:Number(s.contentWidth||620),color:op>0.15?'rgba(255,255,255,.82)':undefined}}>{s.text||''}</p><div className="heroButtons"><Link className="btn" href={s.buttonUrl||'/shop'}>{s.buttonLabel||'Shop now'} <ArrowRight size={17}/></Link>{s.secondaryLabel&&<Link className="btn secondary" href={s.secondaryUrl||'#'}>{s.secondaryLabel}</Link>}</div></div></div></section>}
   if(section.type==='slideshow') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container slideshow"><div className="slideMain"><div className="slideOverlay"><span className="pill">FEATURED</span><h2>{section.blocks?.[0]?.settings?.heading||'Your next campaign starts here.'}</h2><p>{section.blocks?.[0]?.settings?.text||'Add slides, images and calls to action from Theme Studio.'}</p><Link className="btn" href={section.blocks?.[0]?.settings?.buttonUrl||'/shop'}>{section.blocks?.[0]?.settings?.buttonLabel||'Shop now'}</Link></div></div><div className="slideDots">{(section.blocks||[]).slice(0,5).map((b:any,i:number)=><span key={b.id} className={i===0?'active':''}/>)}</div></div></section>
   if(section.type==='product_grid'||section.type==='product_carousel') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container"><div className="sectionHead"><div><span className="muted">SHOP / CURATED</span><h2 className="h2">{s.heading||'Featured products'}</h2>{s.subheading&&<p className="muted">{s.subheading}</p>}</div>{s.showViewAll!==false&&<Link href="/shop" className="btn secondary">View all <ChevronRight size={15}/></Link>}</div><div className="grid productGrid" style={{gridTemplateColumns:`repeat(${Math.min(Number(s.columns||theme.layout.productColumns||4),6)},minmax(0,1fr))`}}>{products.slice(0,Number(s.limit||8)).map(p=><ProductCard key={p.id} p={p}/>)}</div></div></section>
   if(section.type==='collection_grid'||section.type==='collection_carousel') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container"><div className="sectionHead"><div><span className="muted">BROWSE</span><h2 className="h2">{s.heading||'Shop by collection'}</h2>{s.subheading&&<p className="muted">{s.subheading}</p>}</div><Link href="/collections" className="btn secondary">All collections <ChevronRight size={15}/></Link></div><div className="grid collectionGrid" style={{gridTemplateColumns:`repeat(${Math.min(Number(s.columns||4),6)},minmax(0,1fr))`}}>{collections.slice(0,Number(s.limit||4)).map(c=><Link className="card collectionCard" href={`/collections/${c.slug}`} key={c.id}><div className="collectionImage"><img src={c.imageUrl||'/placeholder-product.svg'} alt=""/></div><div><h3>{c.name}</h3><p className="muted">{c.description}</p></div></Link>)}</div></div></section>
   if(section.type==='image_with_text') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className={`container imageTextSection ${s.layout||'image-right'}`}><div className="imageTextMedia" style={s.imageUrl?{backgroundImage:`url(${s.imageUrl})`}:undefined}><div className="mediaShine"/></div><div className="imageTextCopy"><span className="muted">{s.eyebrow||'THE BRAND'}</span><h2 className="h2">{s.heading||'Tell your story'}</h2><p className="body muted">{s.text||''}</p>{s.buttonLabel&&<Link className="btn" href={s.buttonUrl||'#'}>{s.buttonLabel} <ArrowRight size={16}/></Link>}</div></div></section>
   if(section.type==='promo_grid') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container"><div className="sectionHead"><div><span className="muted">FEATURED</span><h2 className="h2">{s.heading||'Shop the edit'}</h2></div></div><div className="promoGrid" style={{gridTemplateColumns:`repeat(${Math.min(Number(s.columns||3),4)},minmax(0,1fr))`}}>{(section.blocks||[]).map((b:any)=><Link href={b.settings.url||'#'} className="promoCard card" key={b.id}><div className="promoMedia" style={b.settings.imageUrl?{backgroundImage:`url(${b.settings.imageUrl})`}:undefined}><span>Explore</span></div><h3>{b.settings.heading}</h3><p className="muted">{b.settings.text}</p></Link>)}</div></div></section>
   if(section.type==='testimonials') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container"><div className="sectionHead"><div><span className="muted">REVIEWS</span><h2 className="h2">{s.heading||'Loved by customers'}</h2>{s.subheading&&<p className="muted">{s.subheading}</p>}</div></div><div className="testimonialGrid">{(section.blocks||[]).map((b:any)=><article className="testimonialCard" key={b.id}><div className="stars">{'★'.repeat(Number(b.settings.rating||5))}</div><p>“{b.settings.quote}”</p><strong>{b.settings.author}</strong><span className="muted">{b.settings.role}</span></article>)}</div></div></section>
   if(section.type==='logo_list') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container logoStrip">{(section.blocks||[]).map((b:any)=><div className="logoTile" key={b.id}>{b.settings.imageUrl?<img src={b.settings.imageUrl} alt={b.settings.alt||''}/>:<span>{b.settings.text||'Brand'}</span>}</div>)}</div></section>
   if(section.type==='faq') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container faqSection"><div><span className="muted">FAQ</span><h2 className="h2">{s.heading||'Frequently asked questions'}</h2></div><div className="faqList">{(section.blocks||[]).map((b:any)=><details key={b.id}><summary>{b.settings.question}<span>+</span></summary><p>{b.settings.answer}</p></details>)}</div></div></section>
   if(section.type==='video') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container videoSection"><div className="videoPoster" style={s.imageUrl?{backgroundImage:`linear-gradient(rgba(0,0,0,.25),rgba(0,0,0,.25)),url(${s.imageUrl})`}:undefined}><span className="playButton"><Play size={20}/></span></div></div></section>
   if(section.type==='rich_text') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container"><div className="richHome"><span className="muted">{s.eyebrow||'ABOUT THE STORE'}</span><h2 className="h2">{s.heading||''}</h2><p className="body muted">{s.text||''}</p></div></div></section>
   if(section.type==='newsletter') return <section className={`storeSection ${anim}`} key={section.id} style={style}><div className="container"><div className={`newsletterHome ${s.background||'primary'}`}><div><span className="muted">NEWSLETTER</span><h2 className="h2">{s.heading||'Stay in the loop'}</h2><p className="muted">{s.text||''}</p></div><div className="newsletterForm"><input className="input" placeholder="Email address"/><button className="btn">{s.buttonLabel||'Subscribe'}</button></div></div></div></section>
   return null
 })}</main><Footer/></>
}

import { db } from '@/lib/prisma'; import { ProductCard } from '@/components/product-card'; import { Footer } from '@/components/footer'; import { ProductStatus } from '@prisma/client';
export default async function Shop({searchParams}:{searchParams:Promise<{q?:string;category?:string;min?:string;max?:string;sort?:string}>}){const sp=await searchParams;const q=sp.q?.trim();const min=Number(sp.min);const max=Number(sp.max);const sort=sp.sort||'newest';const orderBy=sort==='price_asc'?{basePrice:'asc' as const}:sort==='price_desc'?{basePrice:'desc' as const}:{createdAt:'desc' as const};const priceFilter=(Number.isFinite(min)&&min>0)||(Number.isFinite(max)&&max>0)?{basePrice:{...(Number.isFinite(min)&&min>0?{gte:Math.trunc(min*100)}:{}),...(Number.isFinite(max)&&max>0?{lte:Math.trunc(max*100)}:{})}}:{};const products=await db.product.findMany({where:{status:ProductStatus.ACTIVE,...(q?{OR:[{name:{contains:q}},{sku:{contains:q}},{description:{contains:q}}]}:{}),...(sp.category?{category:{slug:sp.category}}:{}),...priceFilter},include:{images:true,category:true},orderBy});const cats=await db.category.findMany({where:{isActive:true},orderBy:{sortOrder:'asc'}});return <><main className="section"><div className="container"><div className="sectionHead"><div><span className="muted">STORE</span><h1 className="h2">Shop</h1><p className="muted">{products.length} products</p></div></div><form className="filterBar"><input className="input" name="q" placeholder="Search products…" defaultValue={q}/><select className="input" name="category" defaultValue={sp.category||''}><option value="">All categories</option>{cats.map(c=><option key={c.id} value={c.slug}>{c.name}</option>)}</select><select className="input" name="sort" defaultValue={sort}><option value="newest">Newest</option><option value="price_asc">Price: low to high</option><option value="price_desc">Price: high to low</option></select><button className="btn secondary">Apply</button></form><div className="grid productGrid">{products.map(p=><ProductCard key={p.id} p={p}/>)}</div>{!products.length&&<div className="card empty"><h3>No products found.</h3><p className="muted">Try a different filter or search term.</p></div>}</div></main><Footer/></>}

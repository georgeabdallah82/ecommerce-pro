import { getCurrentUser } from '@/lib/auth'; import { db } from '@/lib/prisma'; import { redirect } from 'next/navigation'; import { ProductCard } from '@/components/product-card'; import { Footer } from '@/components/footer';
export default async function Wishlist(){const u=await getCurrentUser();if(!u)redirect('/account/login');const items=await db.wishlistItem.findMany({where:{userId:u.id},include:{product:{include:{images:true,category:true}}},orderBy:{createdAt:'desc'}});return <><main className="section"><div className="container"><span className="muted">SAVED</span><h1 className="h2">Wishlist</h1>{!items.length?<div className="card empty"><p className="muted">You haven’t saved anything yet.</p></div>:<div className="grid productGrid">{items.map(x=><ProductCard key={x.id} p={x.product}/>)}</div>}</div></main><Footer/></>}

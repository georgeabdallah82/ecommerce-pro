'use client'

import { useEffect, useMemo, useState } from 'react'
import { BarChart3, RefreshCw, TrendingDown, TrendingUp, Package, Users, ShoppingBag, DollarSign } from 'lucide-react'
import { money } from '@/lib/config'

type Analytics = { periodDays:number; compareDays:number; granularity:'day'|'month'; kpis:any; series:{date:string;revenue:number;orders:number}[]; topProducts:any[]; lowStock:any[] }
const ranges = [{label:'Today',days:1},{label:'7 days',days:7},{label:'30 days',days:30},{label:'90 days',days:90},{label:'12 months',days:365}]

export default function AnalyticsAdmin(){
  const [days,setDays]=useState(30),[data,setData]=useState<Analytics|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState('')
  const load=async()=>{setLoading(true);setError('');try{const r=await fetch(`/api/admin/analytics?days=${days}&compare=${days}`,{cache:'no-store'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to load analytics');setData(d)}catch(e){setError(e instanceof Error?e.message:'Unable to load analytics')}finally{setLoading(false)}}
  useEffect(()=>{load()},[days]); const maxRevenue=useMemo(()=>Math.max(1,...(data?.series||[]).map(x=>x.revenue)),[data])
  const chartLabel=(date:string)=>data?.granularity==='month'?date.slice(0,7):days<=7?date.slice(5):days<=30?date.slice(5):''
  return <div className="analyticsPage">
    <style jsx global>{`.analyticsPage{padding-bottom:24px}.analyticsPage .rangeBar{display:flex;gap:3px;padding:3px;border:1px solid var(--line);background:#fff;border-radius:10px}.analyticsPage .rangeBar button{border:0;background:transparent;border-radius:7px;padding:8px 10px;font-size:11px;font-weight:800;color:var(--muted);cursor:pointer}.analyticsPage .rangeBar button.active{background:var(--soft);color:var(--ink)}.analyticsPage .analyticsKpis{display:grid;grid-template-columns:repeat(5,1fr);gap:14px;margin:18px 0}.analyticsPage .metricCard{padding:18px;display:grid;gap:6px}.analyticsPage .metricIcon{width:34px;height:34px;border-radius:10px;background:var(--soft);display:grid;place-items:center}.analyticsPage .metricCard strong{font-size:25px;letter-spacing:-.03em}.analyticsPage .metricUp,.analyticsPage .metricDown{display:flex;align-items:center;gap:4px;font-size:11px;font-weight:800}.analyticsPage .metricUp{color:#137a3b}.analyticsPage .metricDown{color:#b42318}.analyticsPage .analyticsGrid{display:grid;grid-template-columns:1.45fr .85fr;gap:16px}.analyticsPage .analyticsGrid.bottom{margin-top:16px}.analyticsPage .analyticsCard{padding:0;min-height:300px}.analyticsPage .analyticsCardHead{display:flex;justify-content:space-between;align-items:flex-start;gap:14px;padding:18px 20px;border-bottom:1px solid var(--line)}.analyticsPage .analyticsCardHead h3{margin:0;font-size:15px}.analyticsPage .analyticsCardHead p{margin:4px 0 0;font-size:12px}.analyticsPage .chart{height:270px;padding:20px;display:flex;align-items:end}.analyticsPage .chartBars{width:100%;height:220px;display:flex;align-items:end;gap:4px;border-bottom:1px solid var(--line);overflow:hidden}.analyticsPage .barCol{height:100%;flex:1;min-width:3px;display:flex;flex-direction:column;justify-content:flex-end;align-items:center;gap:5px}.analyticsPage .bar{width:100%;max-width:20px;border-radius:5px 5px 0 0;background:var(--ink);opacity:.86;min-height:3px}.analyticsPage .barCol span{font-size:9px;color:var(--muted);white-space:nowrap}.analyticsPage .rankList{padding:8px 20px}.analyticsPage .rankRow{display:grid;grid-template-columns:24px minmax(0,1fr) auto;gap:10px;align-items:center;padding:12px 0;border-bottom:1px solid var(--line)}.analyticsPage .rankRow:last-child{border-bottom:0}.analyticsPage .rank{width:24px;height:24px;border-radius:7px;background:var(--soft);display:grid;place-items:center;font-size:10px;font-weight:900}.analyticsPage .rankInfo{display:grid;gap:3px;min-width:0}.analyticsPage .rankInfo strong{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.analyticsPage .rankInfo span{font-size:11px}.analyticsPage .insightList{padding:8px 20px}.analyticsPage .insightList>div{padding:14px 0;border-bottom:1px solid var(--line);display:grid;gap:4px}.analyticsPage .insightList>div:last-child{border-bottom:0}.analyticsPage .insightList strong{font-size:14px}.analyticsPage .analyticsLoading{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.analyticsPage .skeleton{height:180px;animation:analyticsPulse 1.3s ease-in-out infinite;background:linear-gradient(90deg,#fff,#f4f4f0,#fff);background-size:200% 100%}@keyframes analyticsPulse{from{background-position:0 0}to{background-position:-200% 0}}@media(max-width:1100px){.analyticsPage .analyticsKpis{grid-template-columns:repeat(3,1fr)}}@media(max-width:800px){.analyticsPage .catalogHead{align-items:flex-start;flex-direction:column}.analyticsPage .rangeBar{width:100%;overflow:auto}.analyticsPage .rangeBar button{white-space:nowrap}.analyticsPage .analyticsKpis{grid-template-columns:repeat(2,1fr)}.analyticsPage .analyticsGrid{grid-template-columns:1fr}}@media(max-width:520px){.analyticsPage .analyticsKpis{grid-template-columns:1fr}.analyticsPage .metricCard strong{font-size:22px}.analyticsPage .chart{padding:14px}.analyticsPage .chartBars{gap:2px}}`}</style>
    <div className="sectionHead catalogHead"><div><span className="muted">ANALYTICS</span><h1 className="h2">Analytics</h1><p className="muted">Understand sales, customers and inventory at a glance.</p></div><div className="inline"><div className="rangeBar">{ranges.map(r=><button key={r.days} className={days===r.days?'active':''} onClick={()=>setDays(r.days)}>{r.label}</button>)}</div><button className="btn secondary" onClick={load} disabled={loading}><RefreshCw size={15}/> Refresh</button></div></div>
    {error&&<div className="alert danger">{error}</div>}
    {loading&&!data?<div className="analyticsLoading"><div className="card skeleton"/><div className="card skeleton"/><div className="card skeleton"/></div>:data&&<>
      <div className="analyticsKpis"><Metric icon={<DollarSign size={18}/>} label="Net sales" value={money(data.kpis.revenue)} change={data.kpis.revenueChange}/><Metric icon={<ShoppingBag size={18}/>} label="Orders" value={data.kpis.orders}/><Metric icon={<BarChart3 size={18}/>} label="Average order" value={money(data.kpis.averageOrderValue)}/><Metric icon={<Package size={18}/>} label="Items sold" value={data.kpis.itemsSold}/><Metric icon={<Users size={18}/>} label="New customers" value={data.kpis.newCustomers}/></div>
      <div className="analyticsGrid"><section className="card analyticsCard"><div className="analyticsCardHead"><div><h3>Sales overview</h3><p className="muted">{data.granularity==='month'?'Net revenue by month':'Net revenue by day'}</p></div><span className="pill">Last {days===365?'12 months':`${days} days`}</span></div><div className="chart"><div className="chartBars">{data.series.map(x=><div className="barCol" key={x.date} title={`${x.date}: ${money(x.revenue)} · ${x.orders} orders`}><div className="bar" style={{height:`${Math.max(3,Math.round((x.revenue/maxRevenue)*100))}%`}}/><span>{chartLabel(x.date)}</span></div>)}</div></div></section>
      <section className="card analyticsCard"><div className="analyticsCardHead"><div><h3>Top products</h3><p className="muted">Best performers by sales</p></div></div><div className="rankList">{!data.topProducts.length?<div className="emptyInline">No sales in this period.</div>:data.topProducts.map((p,i)=><div className="rankRow" key={p.productId}><span className="rank">{i+1}</span><div className="rankInfo"><strong>{p.name}</strong><span className="muted">{p.quantity} units</span></div><strong>{money(p.sales)}</strong></div>)}</div></section></div>
      <div className="analyticsGrid bottom"><section className="card analyticsCard"><div className="analyticsCardHead"><div><h3>Inventory watch</h3><p className="muted">Products that need attention</p></div><a className="textLink" href="/admin/inventory">View inventory</a></div>{!data.lowStock.length?<div className="emptyInline">Inventory looks healthy.</div>:<div className="rankList">{data.lowStock.map(x=><div className="rankRow" key={x.id}><div className="rankInfo"><strong>{x.product}</strong><span className="muted">{x.sku}{x.variant?` · ${x.variant}`:''}</span></div><span className={x.available<=0?'statusPill danger':'statusPill warning'}>{x.available} available</span></div>)}</div>}</section>
      <section className="card analyticsCard"><div className="analyticsCardHead"><div><h3>Quick insights</h3><p className="muted">Useful operational signals</p></div></div><div className="insightList"><div><strong>{data.kpis.revenueChange>=0?'Sales are trending up':'Sales are trending down'}</strong><span className="muted">{Math.abs(data.kpis.revenueChange)}% vs previous period</span></div><div><strong>{data.kpis.orders?Math.round(data.kpis.itemsSold/data.kpis.orders*10)/10:0} items</strong><span className="muted">Average items per order</span></div><div><strong>{data.lowStock.length}</strong><span className="muted">Low-stock items to review</span></div></div></section></div>
    </>}
  </div>
}

function Metric({icon,label,value,change}:{icon:React.ReactNode;label:string;value:React.ReactNode;change?:number}){return <div className="card metricCard"><div className="metricIcon">{icon}</div><span className="muted">{label}</span><strong>{value}</strong>{typeof change==='number'&&<small className={change>=0?'metricUp':'metricDown'}>{change>=0?<TrendingUp size={13}/>:<TrendingDown size={13}/>} {Math.abs(change)}% vs previous</small>}</div>}

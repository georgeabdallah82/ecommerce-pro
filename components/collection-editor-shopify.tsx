'use client'

import { useEffect, useMemo, useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, Check, Eye, GripVertical, Image as ImageIcon, Plus, Save, Search, Trash2, X } from 'lucide-react'

type Product = { id:string; name:string; slug:string; sku?:string|null; status:string; basePrice:number; images?:{url:string}[]; category?:{name:string}|null }
type Collection = { id:string; name:string; slug:string; description?:string|null; imageUrl?:string|null; isActive:boolean; products:{ product:Product }[] }

async function api(path:string, init?:RequestInit){
  const r=await fetch(path,{...init,headers:{'content-type':'application/json',...(init?.headers||{})}})
  const data=await r.json().catch(()=>({}))
  if(!r.ok) throw new Error(data.error||'Request failed')
  return data
}

const styles=`
.collectionEditorShopify{max-width:1280px;margin:0 auto;padding-bottom:32px}.collectionEditorTopbar{position:sticky;top:0;z-index:30;display:flex;align-items:center;justify-content:space-between;gap:18px;padding:14px 0 16px;background:var(--bg,#f7f7f4);border-bottom:1px solid rgba(0,0,0,.06);margin-bottom:18px}.collectionEditorTitle{display:flex;align-items:center;gap:12px}.collectionEditorTitle h1{margin:2px 0 2px;font-size:30px;letter-spacing:-.035em}.collectionEditorGrid{display:grid;grid-template-columns:minmax(0,1fr) 330px;gap:18px}.collectionEditorMain,.collectionEditorSide{display:grid;gap:18px;align-content:start}.collectionCard{padding:18px}.collectionCardHead{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:16px}.collectionCardHead h2,.collectionCard>h2{margin:0;font-size:16px;letter-spacing:-.02em}.collectionCardHead p{margin:5px 0 0;color:#77776f;font-size:12px}.collectionFields{display:grid;gap:14px}.collectionFields label{display:grid;gap:7px;font-size:11px;font-weight:700;color:#66665f}.collectionFields textarea{resize:vertical}.collectionImageField{display:grid;grid-template-columns:76px 1fr;gap:10px;align-items:center}.collectionImagePreview{width:76px;height:76px;border-radius:10px;background:#efefeb;border:1px solid #e0e0da;display:grid;place-items:center;overflow:hidden;color:#9a9a92}.collectionImagePreview img{width:100%;height:100%;object-fit:cover}.assignedList{display:grid;border:1px solid #e5e5df;border-radius:10px;overflow:hidden}.assignedRow{display:grid;grid-template-columns:18px 44px minmax(0,1fr) auto 34px;align-items:center;gap:10px;min-height:66px;padding:9px 12px;background:#fff;border-bottom:1px solid #ededeb}.assignedRow:last-child{border-bottom:0}.assignedRow>svg{color:#aaa9a1}.assignedThumb{width:44px;height:44px;border-radius:8px;background:#f1f1ed;display:grid;place-items:center;overflow:hidden;color:#999}.assignedThumb img{width:100%;height:100%;object-fit:cover}.assignedInfo{min-width:0}.assignedInfo strong{display:block;color:#171717;font-size:13px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.assignedInfo span{display:block;margin-top:3px;color:#7b7b73;font-size:11px}.assignedPrice{font-size:12px;color:#42423d;font-weight:650}.collectionStatus{width:100%;display:flex;align-items:center;gap:11px;padding:12px;border:1px solid #deded8;background:#fff;border-radius:10px;text-align:left;cursor:pointer}.collectionStatus.active{background:#f6fbf7;border-color:#cce4d0}.statusDot{width:9px;height:9px;border-radius:50%;background:#9a9a92;flex:none}.collectionStatus.active .statusDot{background:#28a34a}.collectionStatus strong{display:block;font-size:13px;color:#292924}.collectionStatus small{display:block;margin-top:3px;color:#77776f;font-size:11px}.collectionMetaRow{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:10px 0;border-bottom:1px solid #ededeb;font-size:12px}.collectionMetaRow:last-child{border-bottom:0}.collectionMetaRow span{color:#77776f}.collectionMetaRow code{max-width:200px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.collectionPicker{width:min(680px,94vw);max-height:min(760px,88vh);display:flex;flex-direction:column;padding:18px}.collectionPickerHead,.collectionPickerFoot{display:flex;align-items:center;justify-content:space-between;gap:12px}.collectionPickerHead{padding-bottom:14px}.collectionPickerHead h2{margin:0;font-size:18px}.collectionPickerHead p{margin:4px 0 0;color:#77776f;font-size:12px}.collectionPickerSearch{display:flex;align-items:center;gap:9px;border:1px solid #dfdfd8;border-radius:10px;padding:0 11px;height:42px;color:#77776f;margin-bottom:10px}.collectionPickerSearch input{flex:1;border:0;outline:0;background:transparent;font:inherit}.collectionPickerList{min-height:0;overflow:auto;border:1px solid #e5e5df;border-radius:10px}.pickerProduct{display:grid;grid-template-columns:26px 42px minmax(0,1fr);align-items:center;gap:10px;width:100%;padding:9px 11px;border:0;border-bottom:1px solid #ededeb;background:#fff;text-align:left;cursor:pointer}.pickerProduct:last-child{border-bottom:0}.pickerProduct:hover,.pickerProduct.selected{background:#f5f5f1}.pickerCheck{width:20px;height:20px;border:1px solid #cfcfc7;border-radius:6px;display:grid;place-items:center;color:#fff}.pickerProduct.selected .pickerCheck{background:#171717;border-color:#171717}.pickerThumb{width:42px;height:42px;border-radius:8px;background:#f1f1ed;display:grid;place-items:center;overflow:hidden;color:#999}.pickerThumb img{width:100%;height:100%;object-fit:cover}.pickerInfo{min-width:0}.pickerInfo strong{display:block;font-size:12px;color:#1c1c19;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.pickerInfo small{display:block;margin-top:3px;color:#7b7b73;font-size:10px}.collectionPickerFoot{padding-top:12px}.collectionEditorLoading{padding:50px 10px;color:#77776f}@media(max-width:900px){.collectionEditorGrid{grid-template-columns:1fr}.collectionEditorSide{grid-template-columns:1fr 1fr}.collectionEditorTopbar{top:0}}@media(max-width:620px){.collectionEditorTopbar{align-items:flex-start;flex-direction:column}.collectionEditorTopbar>.inline{width:100%}.collectionEditorTopbar>.inline .btn{flex:1;justify-content:center}.collectionEditorSide{grid-template-columns:1fr}.collectionCard{padding:14px}.assignedRow{grid-template-columns:16px 40px minmax(0,1fr) 34px}.assignedPrice{display:none}.collectionImageField{grid-template-columns:60px 1fr}.collectionImagePreview{width:60px;height:60px}}
`

export default function CollectionEditorShopify({ id }:{ id:string }){
  const [collection,setCollection]=useState<Collection|null>(null)
  const [products,setProducts]=useState<Product[]>([])
  const [q,setQ]=useState('')
  const [loading,setLoading]=useState(true)
  const [saving,setSaving]=useState(false)
  const [error,setError]=useState('')
  const [notice,setNotice]=useState('')
  const [dirty,setDirty]=useState(false)
  const [pickerOpen,setPickerOpen]=useState(false)
  const [selected,setSelected]=useState<string[]>([])

  useEffect(()=>{
    Promise.all([api(`/api/admin/collections/${id}`),api('/api/admin/products?page=1&pageSize=50&sort=name_asc')])
      .then(([c,p])=>{setCollection(c.collection);setProducts(p.rows||[]);setLoading(false)})
      .catch(e=>{setError(e instanceof Error?e.message:'Unable to load collection');setLoading(false)})
  },[id])

  useEffect(()=>{if(collection) setSelected(collection.products.map(x=>x.product.id))},[collection?.id])

  const filteredProducts=useMemo(()=>products.filter(p=>!q||`${p.name} ${p.sku||''}`.toLowerCase().includes(q.toLowerCase())),[products,q])
  const assigned=collection?.products.map(x=>x.product) || []
  function setField(p:Partial<Collection>){setCollection(c=>c?{...c,...p}:c);setDirty(true);setNotice('')}
  function toggleProduct(product:Product){setSelected(current=>current.includes(product.id)?current.filter(id=>id!==product.id):[...current,product.id]);setDirty(true);setNotice('')}
  function removeAssigned(productId:string){setSelected(current=>current.filter(x=>x!==productId));setDirty(true);setNotice('')}
  async function save(){if(!collection)return;setSaving(true);setError('');setNotice('');try{await api(`/api/admin/collections/${id}`,{method:'PATCH',body:JSON.stringify({name:collection.name,slug:collection.slug,description:collection.description||null,imageUrl:collection.imageUrl||null,isActive:collection.isActive,productIds:selected})});const fresh=await api(`/api/admin/collections/${id}`);setCollection(fresh.collection);setSelected(fresh.collection.products.map((x:any)=>x.product.id));setDirty(false);setNotice('Collection saved')}catch(e){setError(e instanceof Error?e.message:'Unable to save collection')}finally{setSaving(false)}}

  if(loading) return <div className="collectionEditorLoading">Loading collection…</div>
  if(!collection) return <div className="empty">{error||'Collection not found.'}</div>

  return <><style>{styles}</style><div className="collectionEditorShopify">
    <div className="collectionEditorTopbar"><div className="collectionEditorTitle"><Link href="/admin/collections" className="iconBtn"><ArrowLeft size={18}/></Link><div><div className="muted tiny">COLLECTION</div><h1>{collection.name||'Untitled collection'}</h1>{dirty&&<span className="muted tiny">Unsaved changes</span>}</div></div><div className="inline"><Link className="btn secondary" href={`/collections/${collection.slug}`} target="_blank"><Eye size={16}/> Preview</Link><button className="btn" disabled={saving||!dirty} onClick={save}>{saving?'Saving…':<><Save size={16}/> Save</>}</button></div></div>
    {(error||notice)&&<div className={error?'alert danger':'alert'}>{error||notice}</div>}
    <div className="collectionEditorGrid"><main className="collectionEditorMain">
      <section className="card collectionCard"><div className="collectionCardHead"><div><h2>Collection details</h2><p>Control how this collection is named and presented in your store.</p></div></div><div className="collectionFields"><label>Name<input className="input" value={collection.name} onChange={e=>setField({name:e.target.value})}/></label><label>Handle<input className="input" value={collection.slug} onChange={e=>setField({slug:e.target.value})}/></label><label>Description<textarea className="textarea" rows={7} value={collection.description||''} onChange={e=>setField({description:e.target.value})}/></label><label>Collection image<div className="collectionImageField"><div className="collectionImagePreview">{collection.imageUrl?<img src={collection.imageUrl} alt=""/>:<ImageIcon size={24}/>}</div><input className="input" placeholder="https://…" value={collection.imageUrl||''} onChange={e=>setField({imageUrl:e.target.value})}/></div></label></div></section>
      <section className="card collectionCard"><div className="collectionCardHead"><div><h2>Products</h2><p>{selected.length} product{selected.length===1?'':'s'} in this collection</p></div><button className="btn" onClick={()=>setPickerOpen(true)}><Plus size={16}/> Add products</button></div>{assigned.length?<div className="assignedList">{assigned.filter(p=>selected.includes(p.id)).map(p=><div className="assignedRow" key={p.id}><GripVertical size={16}/><div className="assignedThumb">{p.images?.[0]?.url?<img src={p.images[0].url} alt=""/>:<span>◎</span>}</div><div className="assignedInfo"><Link href={`/admin/products/${p.id}`}><strong>{p.name}</strong></Link><span>{p.sku||'No SKU'}{p.category?.name?` · ${p.category.name}`:''}</span></div><div className="assignedPrice">{(Number(p.basePrice||0)/100).toFixed(2)}</div><button className="iconBtn" title="Remove" onClick={()=>removeAssigned(p.id)}><Trash2 size={16}/></button></div>)}</div>:<div className="emptyInline">No products yet. Add products to build this collection.</div>}</section>
    </main><aside className="collectionEditorSide"><section className="card collectionCard"><div className="collectionCardHead"><div><h2>Status</h2><p>Control storefront visibility.</p></div></div><button className={collection.isActive?'collectionStatus active':'collectionStatus'} onClick={()=>setField({isActive:!collection.isActive})}><span className="statusDot"/><span><strong>{collection.isActive?'Active':'Inactive'}</strong><small>{collection.isActive?'Visible in the Online Store':'Hidden from the Online Store'}</small></span></button></section><section className="card collectionCard"><h2>Storefront</h2><div className="collectionMetaRow"><span>URL</span><code>/collections/{collection.slug}</code></div><div className="collectionMetaRow"><span>Products</span><strong>{selected.length}</strong></div></section></aside></div>
    {pickerOpen&&<div className="modalOverlay" onClick={()=>setPickerOpen(false)}><div className="card collectionPicker" onClick={e=>e.stopPropagation()}><div className="collectionPickerHead"><div><h2>Add products</h2><p>Select products to include in this collection.</p></div><button className="iconBtn" onClick={()=>setPickerOpen(false)}><X size={17}/></button></div><div className="collectionPickerSearch"><Search size={16}/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products…"/></div><div className="collectionPickerList">{filteredProducts.map(p=>{const checked=selected.includes(p.id);return <button key={p.id} className={checked?'pickerProduct selected':'pickerProduct'} onClick={()=>toggleProduct(p)}><span className="pickerCheck">{checked?<Check size={15}/>:null}</span><span className="pickerThumb">{p.images?.[0]?.url?<img src={p.images[0].url} alt=""/>:<span>◎</span>}</span><span className="pickerInfo"><strong>{p.name}</strong><small>{p.sku||'No SKU'} · {p.status}</small></span></button>})}{!filteredProducts.length&&<div className="empty">No products found.</div>}</div><div className="collectionPickerFoot"><span className="muted">{selected.length} selected</span><div className="inline"><button className="btn secondary" onClick={()=>setPickerOpen(false)}>Cancel</button><button className="btn" onClick={()=>setPickerOpen(false)}>Done</button></div></div></div></div>}
  </div></>
}

import Link from 'next/link'; import { money } from '@/lib/config';
export function ProductCard({p}:{p:any}){return <Link className="card productCard" href={`/product/${p.slug}`}><img className="productImage" src={p.images?.[0]?.url||'/placeholder-product.svg'} alt={p.images?.[0]?.alt||p.name}/><div className="productBody"><span className="muted" style={{fontSize:12}}>{p.category?.name||'Collection'}</span><div className="productName">{p.name}</div><div className="price">{money(p.basePrice)}</div>{p.compareAtPrice&&<div className="comparePrice">{money(p.compareAtPrice)}</div>}</div></Link>}

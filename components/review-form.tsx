'use client'
import { useState } from 'react'
export function ReviewForm({productId}:{productId:string}){const [rating,setRating]=useState(5);const [body,setBody]=useState('');const [title,setTitle]=useState('');const [msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('');const r=await fetch('/api/reviews',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({productId,rating,title,body})});const d=await r.json();setMsg(r.ok?'Thanks! Your review is pending approval.':d.error||'Unable to submit review');if(r.ok){setBody('');setTitle('')}}return <form className="card reviewForm" onSubmit={submit}><h3>Write a review</h3><select className="input" value={rating} onChange={e=>setRating(Number(e.target.value))}><option value="5">5 stars</option><option value="4">4 stars</option><option value="3">3 stars</option><option value="2">2 stars</option><option value="1">1 star</option></select><input className="input" placeholder="Title" value={title} onChange={e=>setTitle(e.target.value)}/><textarea className="textarea" rows={4} placeholder="Your experience" value={body} onChange={e=>setBody(e.target.value)}/><button className="btn">Submit review</button>{msg&&<div className="muted">{msg}</div>}</form>}

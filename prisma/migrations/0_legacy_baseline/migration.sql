-- Legacy production database baseline marker.
-- This database predates Prisma Migrate. The production bootstrap script records
-- this migration as applied after creating the migration history table, so later
-- migrations can be applied without attempting to recreate the legacy schema.
SELECT 1;
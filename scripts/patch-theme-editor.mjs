// Legacy compatibility stub.
// Theme builds no longer mutate source files during compilation.
// The theme runtime and editor are rebuilt as normal tracked source code.
console.log('Legacy theme patcher disabled')
